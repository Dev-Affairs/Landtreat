import { DynamicEventDirective } from './dynamic-event.directive';

describe('DynamicEventDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicEventDirective();
    expect(directive).toBeTruthy();
  });
});
