import { AuthFormValidationDirective } from './auth-form-validation.directive';

describe('AuthFormValidationDirective', () => {
  it('should create an instance', () => {
    const directive = new AuthFormValidationDirective();
    expect(directive).toBeTruthy();
  });
});
